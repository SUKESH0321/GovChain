{
  "name": "govchain-blockchain",
  "version": "0.1.0",
  "private": true,
  "description": "GovChain blockchain module (Stage 2.1) - local Ethereum-compatible development environment (Hardhat + Solidity + ethers.js)",
  "type": "module",
  "scripts": {
    "node": "hardhat node",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network localhost"
  },
  "dependencies": {
    "ethers": "^6.17.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^4.1.0",
    "hardhat": "^3.17.0"
  },
  "engines": {
    "node": ">=22.13.0"
  }
}
